# code-reader
an IDE to just read code and understand it organically

Dependencies:
Install the following
`sudo apt install pkg-config libgtk-3-dev libxcb-shape0-dev libxcb-xfixes0-dev libxkbcommon-dev`